---
"agent-react-devtools": minor
---

**MCP Server**

Expose all DevTools functionality as MCP tools for AI agent integration via stdio transport.

- **10 tools** — `status`, `get_tree`, `get_component`, `find_components`, `count_components`, `profile_start`, `profile_stop`, `profile_report`, `profile_slow`, `profile_rerenders`
- **Stdio transport** — Run with `agent-react-devtools serve-mcp`
